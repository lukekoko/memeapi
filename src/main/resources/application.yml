reddit:
  clientId:
  clientSecret:
  userAgent: SpringMemeApi
  url: "https://www.reddit.com/api/v1/access_token"

spring:
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      password:
      timeout: 60000
  docker:
    compose:
      enabled: true

logging:
  level:
    root: info

